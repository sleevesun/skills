# 贡献指南

感谢你改进这些 Skills。

提交前请确认：

1. `SKILL.md` 的 YAML frontmatter 只包含 `name` 和 `description`。
2. Skill 名称使用小写字母、数字和连字符，目录名与 `name` 一致。
3. 不提交个人绝对路径、内部域名、账号、密钥、Cookie、会话或真实私人素材。
4. 不提交 `node_modules`、缓存、备份文件、生成的主题包或临时导出。
5. 新增或修改脚本时运行相应测试；修改 Skill 后运行 `quick_validate.py`。
6. 说明行为变化、验证方式和仍然存在的环境限制。

仓库级文档放在根目录。Skill 目录只保留执行所需的 `SKILL.md`、`agents/`、`scripts/`、`references/` 和 `assets/`。

